### TopSecret
* Challenge description: "Find out the admin's secrets."
* Requirements: Use heartbleed script to get leaked memory from server. There should be an admin PHPSESSID in the memory at some point. Use this as your PHPSESSID and you can then read the admin's secrets which should have the flag.

